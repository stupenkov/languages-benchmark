# Hello World (Node.js)

Minimal Node.js hello world project.

Run locally:

```bash
npm install
npm start
```

Docker:

```bash
docker build -t hello-world-node .
docker run --rm hello-world-node
```
